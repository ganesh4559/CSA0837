password =input("enter the password:\n")
if (len(password )>=8 and 
   any (c.isupper() for c in password) and
   any(c.islower() for c in password ) and
   any (c.isdigit()  for c in password)):
  print("valid password")
else:
  print("invalid assword ")
