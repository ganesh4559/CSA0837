numbers = [4, -1, 2, 1, -5, 4]

max_sum = numbers[0]
current_sum = 0

for num in numbers:
    current_sum += num
    if current_sum > max_sum:
        max_sum = current_sum
    if current_sum < 0:
        current_sum = 0

print("Largest subsequence sum is:", ma
